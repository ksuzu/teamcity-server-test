import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class ChromeInitializer extends ChromeDriver{

    private WebDriver driver;

    public ChromeInitializer()
    {
        super();
    }
}